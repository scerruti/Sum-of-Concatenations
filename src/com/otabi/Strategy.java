package com.otabi;

/**
 *
 */
public interface Strategy {
    /**
     * Returns the sum of the values of the string concatenation of all
     * elements of a with themselves.
     *
     * @param a an array of positive integer values
     * @return sum of values of the string concatenation
     */
    long solution(int[] a);
}
